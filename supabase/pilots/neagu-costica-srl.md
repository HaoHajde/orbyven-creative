# ORBYVEN Pilot #002 — Neagu Costică SRL

Pilot workspace created in the ORBYVEN Supabase project.

## Organization

- name: `Neagu Costică SRL`
- slug: `neagu-costica-srl`
- locale: `ro-RO`
- timezone: `Europe/Bucharest`
- greeting: `Costică`

## Initial modules

The lean pilot starts with:

- Clienți & cereri (`leads`)
- Lucrări & taskuri (`tasks`)
- Programări (`calendar`)
- Oferte & devize (`estimates`)

`overview` is part of ORBYVEN Core and is always available.

## Access

The first authenticated ORBYVEN account is temporarily attached as `owner` for development and preview. A dedicated client account can later be added as another `organization_members` row without changing any module code.

Do not put user UUIDs, passwords, service-role keys, or other secrets in this file.
